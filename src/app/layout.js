import "./globals.css";

export const metadata = {
    title: "Land Passport",
    description: "Your land, understood. The bioregional platform for landowners.",
};

export default function RootLayout({ children }) {
    return children;
}
